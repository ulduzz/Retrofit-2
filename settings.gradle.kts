pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Retrofit3"
include(":app")
include(":entities")
include(":features:account")
include(":feautures:flights")
include(":features:account")
include(":features:flights")
include(":data")
include(":data")
include(":data")
include(":domain")
include(":domain")
include(":domain")
include(":domain")
include(":data")
include(":common")
include(":network")
